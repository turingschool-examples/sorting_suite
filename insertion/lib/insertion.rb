require 'pry'

class Insertion

  def sort(array)
    to_insert = array.shift
    # compare against sorted list
    until done
      if to_insert > sorted_list[n]
        # next
      else
        # insert number
      end



    end
  end

end
