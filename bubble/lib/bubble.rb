class Bubble
  def sort(array)
    # Determine the length of the array
    # Loop through the array starting from the zero index.
      # Determine whether the first element in the array is larger than the second element in the array
        # If it is larger, swap.
      # Determine wehther the second element in the array is larger than the third element in the array
        # If it is larger, swap.
      # Iterate through the array until the array is sorted lowest to highest. 
  end
end
